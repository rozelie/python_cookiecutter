#!/bin/sh

# Format source code.

set -euo pipefail

cd "$(dirname "$0")/.."

source scripts/_global
activate_venv

echo_step "Running black"
black ${SRC_DIR} ${TESTS_DIR}

echo_step "Running isort"
isort ${SRC_DIR} ${TESTS_DIR}
